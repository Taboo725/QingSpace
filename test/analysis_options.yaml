include: ../analysis_options.yaml

linter:
  rules:
    # Tests state arguments explicitly even when they match the default, because
    # the value under test is part of what the case documents.
    avoid_redundant_argument_values: false
